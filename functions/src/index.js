import { onRequest } from 'firebase-functions/v2/https'; import { defineSecret } from 'firebase-functions/params'; import { GoogleGenAI } from '@google/genai'; import { getAuth } from 'firebase-admin/auth'; import { initializeApp } from 'firebase-admin/app'; import { z } from 'zod';
initializeApp(); const geminiKey=defineSecret('GEMINI_API_KEY'); const schema=z.object({messages:z.array(z.object({role:z.enum(['user','assistant']),content:z.string().min(1).max(30000)})).min(1).max(30),mode:z.string().max(40).default('chat'),language:z.string().max(40).default('auto')});
export const api=onRequest({secrets:[geminiKey],cors:true,timeoutSeconds:300,memory:'512MiB'},async(req,res)=>{if(!req.path.endsWith('/gemini/stream')||req.method!=='POST')return res.status(404).json({message:'Not found'});const token=req.headers.authorization?.replace(/^Bearer\s+/i,'');if(!token)return res.status(401).json({message:'Authentication required'});try{await getAuth().verifyIdToken(token)}catch{return res.status(401).json({message:'Invalid session'})}const parsed=schema.safeParse(req.body);if(!parsed.success)return res.status(400).json({message:'Invalid request'});res.setHeader('Content-Type','text/event-stream; charset=utf-8');res.setHeader('Cache-Control','no-cache, no-transform');const ai=new GoogleGenAI({apiKey:geminiKey.value()});try{const {messages,mode,language}=parsed.data;const stream=await ai.models.generateContentStream({model:process.env.GEMINI_MODEL||'gemini-2.5-flash',contents:messages.map(m=>({role:m.role==='assistant'?'model':'user',parts:[{text:m.content}]})),config:{systemInstruction:`You are DevPilot AI. Mode: ${mode}. Preferred language: ${language}. Return secure production-ready software guidance in Markdown.`,temperature:.35,maxOutputTokens:8192}});for await(const chunk of stream){if(chunk.text)res.write(`data: ${JSON.stringify({text:chunk.text})}\n\n`)}res.end()}catch(e){console.error(e);res.write(`data: ${JSON.stringify({error:'Gemini generation failed.'})}\n\n`);res.end()}});
