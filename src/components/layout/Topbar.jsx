import { Menu, Moon, Search, Sun } from 'lucide-react';
import { useDispatch } from 'react-redux';
import { useTheme } from '../../context/ThemeContext';
import { useAuth } from '../../context/AuthContext';
import { setCommandOpen, toggleSidebar } from '../../store/uiSlice';
export function Topbar() { const dispatch = useDispatch(); const { theme, setTheme } = useTheme(); const { user } = useAuth(); return <header className="sticky top-0 z-30 flex h-16 items-center justify-between border-b border-[var(--border)] bg-[color:var(--background)]/80 px-4 backdrop-blur-xl lg:px-6"><div className="flex items-center gap-3"><button className="focus-ring rounded-xl p-2 lg:hidden" onClick={() => dispatch(toggleSidebar())}><Menu/></button><button onClick={() => dispatch(setCommandOpen(true))} className="focus-ring flex w-[min(50vw,420px)] items-center gap-3 rounded-xl border border-[var(--border)] bg-[var(--surface)] px-3 py-2 text-left text-sm text-muted"><Search size={17}/><span className="truncate">Search tools, prompts, pages…</span><kbd className="ml-auto hidden rounded border border-[var(--border)] px-2 py-0.5 text-xs sm:block">Ctrl K</kbd></button></div><div className="flex items-center gap-2"><button className="focus-ring rounded-xl p-2.5 hover:bg-violet-500/10" onClick={() => setTheme(theme === 'dark' ? 'light' : 'dark')} aria-label="Toggle theme">{theme === 'dark' ? <Sun size={19}/> : <Moon size={19}/>}</button><div className="grid h-9 w-9 place-items-center rounded-full bg-gradient-to-br from-violet-500 to-blue-500 font-bold text-white" title={user?.email}>{(user?.displayName || user?.email || 'U')[0].toUpperCase()}</div></div></header>; }
